# CLAUDE.md

## 项目约束（记忆）

### 修改已有代码实现方式必须先征得同意

任何关于已有代码实现方式修改的操作必须向我进行说明，在得到我的允许之后才可以执行。

具体而言：如果某个功能出现了 bug 需要修改，你没有在原来的代码逻辑上修复，而是想改用另一种实现方式来实现，这是不允许的。你必须先询问我是否可以改为另一种实现。

例如：原本的实现使用了 Kafka，如果你想改为使用 RabbitMQ，这个操作必须先获得我的允许；否则只允许你在原有的 Kafka 逻辑上来修复 bug。

总结：只允许在原有实现逻辑的基础上修复问题；任何技术方案或实现方式的变更（例如替换中间件、框架、算法等）都必须先征得我的明确同意。
